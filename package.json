{
  "private": true,
  "workspaces": [
    "packages/web",
    "packages/infra"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -pt --all run build",
    "test": "yarn workspace web test"
  }
}
